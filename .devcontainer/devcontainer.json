{
  "name": "data-platform",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "workspace",
  "workspaceFolder": "/workspace",
  "postCreateCommand": "python3 -m ensurepip --upgrade && python3 -m pip install --upgrade pip && pip install -r requirements.txt",
  "forwardPorts": [8000, 8080],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python","ms-azuretools.vscode-docker"]
    }
  }
}